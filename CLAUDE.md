# san_sw — 프로젝트 운영 규칙 (영구 기억)

> 이 파일은 세션/컨테이너가 초기화돼도 규칙이 유지되도록 만든 영구 메모입니다.
> Claude는 매 작업마다 이 규칙을 준수합니다.

## 프로젝트 목표
브로케이드(Brocade) SAN switch 관리 프로그램.
- 전세계 데이터센터에 분산된 Brocade SAN switch 모니터링
- 포트 사용율, 포트 속도, 기타 관리 기능
- 포트 사용율 / 사용중 포트 수 / 비어있는(미사용) 포트 수량 집계

## 커뮤니케이션 규칙
- 모든 답변/설명은 **한국어**로 작성한다.
- 사실·팩트에 기반해 정직하게 말한다. 모르면 모른다고, 틀렸으면 틀렸다고 한다.
- 자존심·고집으로 우기지 않는다. 목표는 세상을 개선하는 것이지 논쟁에서 이기는 게 아니다.
- 측정으로 확인 가능하면 추측하지 말고 측정한다. 결과를 과장하지 않는다.
- 사용자에게 듣기 좋은 말보다 정확한 말을 우선한다.
- 엔지니어 관점에서 사실근거한 작업만 수행한다. 사용자를 달래기 위한 거짓말 금지.

## 응답 포맷 규칙
- **작업 시작 시 난이도 표시**: 응답 맨 앞에 한 줄.
  형식: `난이도: 낮음/보통/높음 — Sonnet 적합 | Opus 권장`
  (단순 편집·UI·엔드포인트 = 낮음/보통 → Sonnet,
   아키텍처·동시성·대규모 리팩터·미해결 버그 = 높음 → Opus)
- **새 작업 요청 시 진행상태 표**: 난이도 다음에 "작업 현황" 표.
  열: `작업 | 상태 | 비고`, 상태: ✅ 완료 / 🔄 진행중 / ⏳ 대기.
  현재 진행 중 + 추가로 해야 할 작업(미릴리스 포함)을 한 표에 정리.
- **`.` 입력 = 생존확인**: 사용자가 `.` 만 보내면 "살아있냐"는 뜻.
  현재 작업중 리스트 + 작업 예정 리스트를 보여준다.

## 제품/배포 규칙
- **웹페이지에 항상 버전 명시** (UI에 버전 표시 필수).
- **자동 업그레이드 기능** 구현/유지.
- **PR 자동화**: 작업 완료 시 별도 요청 없이 PR 생성/갱신 (draft).
- **PR 완료 시 GitHub 다운로드 링크 자동 안내**: 푸시/머지 등 PR 작업이 끝나면
  요청을 기다리지 말고 자동으로 GitHub 다운로드 링크를 함께 알려준다.

## Git 규칙
- 개발 브랜치: `claude/brave-fermi-pqouq6`
- 푸시: `git push -u origin <branch>`, 네트워크 실패 시 지수 백오프 재시도.
- 작업 완료 시 draft PR 생성.
